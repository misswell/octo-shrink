server {
    listen 80;
    listen [::]:80;
    server_name octoshrink.liuguofeng.com;

    location ^~ /.well-known/acme-challenge/ {
        root /home/wwwroot/default;
        default_type text/plain;
        try_files $uri =404;
    }

    location / {
        return 404;
    }
}
